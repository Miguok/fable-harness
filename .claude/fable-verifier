# 本 repo 的權威驗證指令（goal gate 讀，協議 §4b-1 第 5 條）。
#
# 這裡列出的指令一旦變綠，goal gate 就把當前那段目標視為達成——連還沒各自
# 轉綠的窄範圍測試也一併收掉。它存在的理由是：除錯與突變測試的節奏必然是
# 「跑窄的 → 紅 → 修好 → 用全套驗 → 綠」，而綠燈落在另一個鍵上。
#
# 為什麼由 repo 宣告、而不是讓程式推論「寬的涵蓋窄的」：那個推論版本寫過，
# 抗辯實測出六種會把**真紅燈**靜默清掉的情況（先綠後紅、`pytest tests` 沒有
# 尾斜線、跨工具、`-k` 過濾、`--ignore`、`cd` 到別的專案），已退回。宣告是
# 一次性的設定，猜錯的可能性歸零。
#
# 一行一條，`#` 開頭是註解。比對時 shell 雜訊（`cd x &&`、管線尾巴）會被剝掉，
# 所以這裡寫最乾淨的形式即可。
python -m pytest tests/ -q
