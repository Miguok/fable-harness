# 這個 repo 自己的接線守衛（組件 4；用法見 INSTALL.md 步驟 10）
#
# 只放「斷言某個東西在執行路徑上」的檢查，不放功能測試——整份清單要留在秒的
# 量級，慢的閘門會被繞過，而被繞過的閘門等於沒有。全套 200 條測試約 10 秒，
# 太慢；這裡只列兩條通則守衛（實測約 1.9 秒）：
#
#   D9  .claude/hooks/ 下每一支 Claude hook 都必須出現在健檢清單裡
#       ——少一支，那支 hook 對健檢就是隱形的
#   D11 INSTALL.md 步驟 5 的六列註冊表必須與健檢清單逐項相同
#       ——文件與程式碼漂移時，兩邊都不會叫
#
# 全套回歸仍是 push 前的責任，本閘只保證「接線」這一類不會漏。
#
# 為什麼這個 repo 自己也要接：它到 v1.3.0 為止都在守別人、自己沒接線——
# 而「做了沒接線」正是這道閘存在的理由。

python -m pytest tests/test_fable_doctor.py -q -k "d9 or d11"
